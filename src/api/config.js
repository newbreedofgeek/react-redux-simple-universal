// we abstract this here so in future we can export api config based on ENV (e.g. dev, stag, prod etc)
export const apiProtocol = 'https';
export const apiRoot = apiProtocol + '://private-c1cb6-stationmanager.apiary-mock.com';
