import { apiRoot } from './config';

// all api related to stations model live here
export const getStations = apiRoot + '/stations';

// [put], [delete] indicated the http request type, e.g.
// putStations
// deleteStations
